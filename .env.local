# Local development settings
# Copy to .env to use: cp .env.local .env

DEBUG=true
DJANGO_SECRET_KEY=local-dev-secret-key-not-for-production
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000

# Leave this commented out to use SQLite (recommended for local dev)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/splattopblog

# Keep CSP in report-only mode during local development.
# CSP_ENFORCE=false
