# Local development settings
# Copy to .env to use: cp .env.local .env

DEBUG=true
DJANGO_SECRET_KEY=local-dev-secret-key-not-for-production
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000

# Leave these commented out to use SQLite (recommended for local dev)
# SQL_HOST=localhost
# SQL_PORT=5432
# SQL_DATABASE=splattopblog
# SQL_USER=postgres
# SQL_PASSWORD=postgres
